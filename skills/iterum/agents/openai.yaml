interface:
  display_name: "Iterum"
  short_description: "Inject tool context before repeat failures"
  default_prompt: "Use $iterum before brittle tool calls so schemas, playbooks, and recent observations are available in context."
