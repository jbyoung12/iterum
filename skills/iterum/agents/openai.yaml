interface:
  display_name: "Iterum"
  short_description: "Avoid repeated Kalshi tool failures"
  default_prompt: "Use $iterum before risky Kalshi tool calls so repeated mistakes are corrected from Redis-backed memory."
